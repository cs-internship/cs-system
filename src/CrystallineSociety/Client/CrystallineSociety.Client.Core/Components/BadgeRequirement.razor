@using CrystallineSociety.Shared.Dtos.BadgeSystem
@inherits AppComponentBase

    @{string[] parts = @Requirement?.RequirementStr.Split('*');}

    @if (parts.Length > 1)
    {
        string textBeforeAsterisk = parts[0];
        string textAfterAsterisk = parts[1];
        <div class="border rounded p-2 pt-1 pb-1">@textBeforeAsterisk
        <span class="badge badge-secondary bg-secondary">@textAfterAsterisk</span>
        </div>
    }
       else
    {
        string textBeforeAsterisk = parts[0];
        string textAfterAsterisk = "1";
        <div class="border rounded p-2 pt-1 pb-1">@textBeforeAsterisk
        <span class="badge badge-secondary bg-secondary">@textAfterAsterisk</span>
        </div>
    }


