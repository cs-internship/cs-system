@using CrystaLearn.Shared.Dtos.Crysta
@attribute [Route(Urls.Crysta.DocsPageRoutePattern + "/{*DocPath}")]
@attribute [Route("{culture?}" + Urls.Crysta.DocsPageRoutePattern)]
@inherits AppPageBase

<PageTitle>@Localizer[nameof(AppStrings.TermsPageTitle)]</PageTitle>
<CascadingValue Value="BitDir.Ltr">
    <BitSplitter 
        FirstPanelMinSize="150" 
        FirstPanelSize="300" 
        FirstPanelMaxSize="500"
        SecondPanelSize="2000">
        <FirstPanel>
            <BitStack>

                @if (IsLoadingTree)
                {
                    <BitEllipsisLoading Style="align-self:center;" />
                }
                else
                {
                    <BitLabel Typography="BitTypography.Body1">Documents</BitLabel>
                    <BitNav Items="DocumentsTree"
                            Style="width: 100%;"
                            OnItemClick="(BitNavItem item) => OnNavItemClicked(item)" />
                }

            </BitStack>

        </FirstPanel>
        <SecondPanel>
            <BitStack Horizontal>
                <BitSpacer Width="5"></BitSpacer>
                <DocumentComponent 
                    CrystaUrl="@CurrentCrystaUrl"
                    />
            </BitStack>
        </SecondPanel>
    </BitSplitter>
</CascadingValue>
